
## 2026-03-05 06:41 HKT - Refactoring & Automated Testing
- Refactored `game.js` into smaller modules:
  - Extracted `GameSave` to `js/game/save.js` (Fixed bug: chapter index was off by 1 on game load).
  - Extracted `PrologueDisplay` to `js/game/prologue.js`.
  - Moved enemy phase logic to `AITurn` inside existing `js/game/ai-turn.js`.
  - Fixed map menu "End Turn" bug where `game.state` was stuck as `mapMenu`.
  - Added `UI.showConfirm` to `ui.js` with `onCancel` handler for Map Menu "Quit Game" feature to avoid locking the game state.
- Fixed enemy unit sprites not resetting direction/selection after acting in `ai-turn.js`.
- Refactored `var` to `const`/`let` in `ui.js`, `battle.js`, `combat.js`, `cursor.js`, `map.js`, `main.js`, and `sprites.js`.
- Discovered and utilized Playwright via system exec to automatically smoke test the game backend. Caught and fixed an `Assignment to constant variable` bug inside `sprites.js` loop indices.
- Pending task: Change "Map Browse" menu item into a mini-map/zoom out view instead of just scrolling around.
- Pending task: Refactor `sprites.js` to remove duplicate procedural rendering logic.
